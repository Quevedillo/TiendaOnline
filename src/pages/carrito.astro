---
import PublicLayout from '@layouts/PublicLayout.astro';
import CartSlideOver from '@components/islands/CartSlideOver';
---

<PublicLayout title="Carrito - KicksPremium">
  <section class="py-12 bg-brand-dark min-h-[60vh]">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-4xl font-display font-bold text-white uppercase tracking-tight mb-8">
        Tu Carrito 🛒
      </h1>

      <div class="bg-brand-gray p-8 text-center">
        <div class="text-6xl mb-4">👟</div>
        <p class="text-neutral-300 text-lg">
          El carrito se maneja mediante el panel deslizante.
        </p>
        <p class="text-neutral-500 text-sm mt-4">
          Haz clic en el icono del carrito <span class="text-brand-red">🛒</span> en la esquina superior derecha para abrirlo.
        </p>
        <a 
          href="/productos" 
          class="inline-block mt-8 bg-brand-red text-white px-8 py-4 font-bold uppercase hover:bg-brand-orange transition-all hover:scale-105"
        >
          Explorar Kicks →
        </a>
      </div>
    </div>
  </section>

  <CartSlideOver client:load />
</PublicLayout>
