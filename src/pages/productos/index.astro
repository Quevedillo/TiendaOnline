---
import PublicLayout from '@layouts/PublicLayout.astro';
import { supabase } from '@lib/supabase';
import type { Product } from '@lib/supabase';

// Fetch all products
const { data: products, error } = await supabase
  .from('products')
  .select('*')
  .order('created_at', { ascending: false });

if (error) {
  console.error('Error fetching products:', error);
}

const productsList: Product[] = products || [];
---

<PublicLayout title="Catálogo - KicksPremium" description="Explora nuestra colección completa de sneakers premium">
  {/* Header */}
  <section class="bg-brand-black border-b border-brand-gray">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-4">
        <div>
          <div class="text-brand-red font-bold uppercase text-sm tracking-wider mb-2">Colección completa</div>
          <h1 class="text-4xl md:text-5xl font-display font-bold text-white uppercase tracking-tight">
            Todos los Kicks
          </h1>
        </div>
        <p class="text-neutral-500">
          {productsList.length} sneakers disponibles
        </p>
      </div>
    </div>
  </section>

  {/* Filters Bar */}
  <section class="bg-brand-gray border-b border-brand-black sticky top-16 z-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <div class="flex flex-wrap gap-3">
        <button class="bg-brand-red text-white px-4 py-2 text-sm font-bold uppercase">Todos</button>
        <a href="/categoria/basketball" class="bg-brand-dark text-white/70 px-4 py-2 text-sm font-bold uppercase hover:bg-brand-red hover:text-white transition-colors">Basketball</a>
        <a href="/categoria/lifestyle" class="bg-brand-dark text-white/70 px-4 py-2 text-sm font-bold uppercase hover:bg-brand-red hover:text-white transition-colors">Lifestyle</a>
        <a href="/categoria/running" class="bg-brand-dark text-white/70 px-4 py-2 text-sm font-bold uppercase hover:bg-brand-red hover:text-white transition-colors">Running</a>
        <a href="/categoria/limited-editions" class="bg-brand-dark text-white/70 px-4 py-2 text-sm font-bold uppercase hover:bg-brand-red hover:text-white transition-colors">🔥 Limited</a>
      </div>
    </div>
  </section>

  {/* Products Grid */}
  <section class="py-12 bg-brand-dark">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      {productsList.length > 0 ? (
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
          {productsList.map((product) => (
            <a href={`/productos/${product.slug}`} class="group bg-brand-gray overflow-hidden hover:scale-[1.02] transition-all">
              {/* Image */}
              <div class="aspect-square bg-brand-dark relative overflow-hidden">
                {product.is_limited_edition && (
                  <div class="absolute top-3 left-3 bg-brand-red text-white text-xs font-bold px-2 py-1 z-10 uppercase">
                    🔥 Limited
                  </div>
                )}
                {product.stock <= 5 && product.stock > 0 && (
                  <div class="absolute top-3 right-3 bg-brand-orange text-white text-xs font-bold px-2 py-1 z-10 uppercase">
                    Últimas {product.stock}
                  </div>
                )}
                <img 
                  src={product.images?.[0] || 'https://via.placeholder.com/400x400?text=Sneaker'}
                  alt={product.name}
                  class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                />
              </div>
              {/* Info */}
              <div class="p-4">
                <div class="text-brand-red text-xs font-bold uppercase tracking-wider mb-1">
                  {product.brand}
                </div>
                <h3 class="text-white font-bold group-hover:text-brand-red transition-colors line-clamp-2">
                  {product.name}
                </h3>
                <div class="flex items-center justify-between mt-3">
                  <div class="flex items-center gap-2">
                    <span class="text-white font-bold text-lg">
                      ${(product.price / 100).toFixed(0)}
                    </span>
                    {product.original_price && product.original_price > product.price && (
                      <span class="text-neutral-500 line-through text-sm">
                        ${(product.original_price / 100).toFixed(0)}
                      </span>
                    )}
                  </div>
                  <span class="text-brand-red text-sm">→</span>
                </div>
              </div>
            </a>
          ))}
        </div>
      ) : (
        <div class="text-center py-20">
          <div class="text-6xl mb-4">👟</div>
          <p class="text-neutral-500 text-lg">
            No hay sneakers disponibles en este momento.
          </p>
          <a
            href="/"
            class="inline-block mt-6 bg-brand-red text-white px-6 py-3 font-bold uppercase hover:bg-brand-orange transition-colors"
          >
            Volver al inicio
          </a>
        </div>
      )}
    </div>
  </section>
</PublicLayout>
